"""Gemini에 보내는 두 단계 프롬프트.

GUARD 한 줄을 두 프롬프트 모두에 넣어, 입력이 비었을 때
사람에게 붙여넣기를 요청하지 않고 '오늘은 새 메시지 없음'만 출력하게 한다.
(실제로는 main.py가 코드 레벨에서도 빈 입력을 먼저 차단한다 — 이중 안전장치.)
"""

GUARD = (
    "[중요] 이 입력은 사람이 아니라 자동 파이프라인에서 넘어온 것이다. "
    "입력이 비어 있거나 정리할 메시지가 하나도 없으면, 사용자에게 무엇을 붙여넣어 달라고 "
    "요청하지 말고 정확히 '오늘은 새 메시지 없음' 한 줄만 출력하라."
)

# --- 1차: 채널 하나의 24시간 글 정리 ---
PER_CHANNEL = """역할: 너는 텔레그램 투자 채널 하나의 지난 24시간 글을 정리하는 분석가다.
아래는 [채널명] 채널의 메시지(텍스트 + 차트 이미지)다. 핵심만 구조화해 정리해라.

{guard}

[입력 형식]
- 각 메시지는 `--- HH:MM ---` 로 구분된다.
- 차트/사진은 해당 메시지 바로 뒤에 첨부돼 있다.

[규칙]
1. 광고·홍보·가입유도·잡담·인사말은 제외.
2. 원문의 핵심 숫자(가격, %, 목표가 등)와 종목명·티커는 절대 바꾸지 말고 그대로 보존.
3. 요약은 하되 의미를 왜곡하거나 없는 내용을 만들지 마라.
4. 차트 이미지는 '보이는 것만' 1~2줄로: 종목/지수, 추세, 지지·저항, 거래량, 패턴.
   이미지에서 확인 안 되는 수치는 추측 금지. 애매하면 "확실하지 않음" 표기.
5. 각 항목에 원문 시간(HH:MM) 보존.

[출력 형식]
채널: [채널명]
- HH:MM | 핵심 내용(숫자·종목명 보존) | 차트: (있으면 차트 설명)
- ...
(중요하지 않은 건 생략. 남길 게 없으면 "특이사항 없음")
""".replace("{guard}", GUARD)


# --- 한방 호출: 여러 채널 원문을 한 번에 받아 최종 다이제스트 생성 ---
# (무료 등급 호출 횟수 한도 때문에 채널별로 따로 부르지 않고 묶어서 1회 호출)
COMBINED = """역할: 너는 여러 텔레그램 투자 채널의 지난 24시간 글(텍스트 + 차트 이미지)을 받아,
내가 출근길에 폰으로 빠르게 읽을 최종 다이제스트를 만드는 분석가 겸 에디터다.

{guard}

[입력 형식]
- 채널은 `### 채널: 이름` 으로 구분된다.
- 각 메시지는 `--- HH:MM ---` 로 구분되고, 차트/사진은 해당 메시지 바로 뒤에 첨부돼 있다.

[규칙]
1. 광고·홍보·가입유도·잡담·인사말은 제외.
2. 원문의 핵심 숫자(가격, %, 목표가 등)와 종목명·티커는 절대 바꾸지 말고 그대로 보존.
3. 요약하되 의미를 왜곡하거나 없는 내용을 만들지 마라.
4. 같은 내용이 여러 채널에 겹치면 하나로 합치고 끝에 "(여러 채널)" 표시.
5. 차트 이미지는 '보이는 것만' 반영: 종목/지수·추세·지지/저항·거래량·패턴. 확인 안 되면 추측 금지, 애매하면 "확실하지 않음".
6. 각 항목에 원문 시간(HH:MM) 보존. 중요도 순. 안 중요한 건 과감히 생략.

[출력 형식] (한국어, 폰에서 읽기 좋게 짧고 스캔하기 쉽게)

📌 오늘의 핵심 (가장 중요한 3~5개, 한 줄씩)
- ...

📁 주제별 상세  (예: 반도체 / 매크로 / 환율 / 채권 / 개별종목)
[주제]
- HH:MM  내용 (차트 설명 포함) (겹치면 "(여러 채널)")
- ...

⚠️ 확인 필요 ("확실하지 않음" 항목들)
- ...

[길이] 항목당 1~2줄. 간결하게.
""".replace("{guard}", GUARD)


# --- 2차: 채널별 1차 요약을 합쳐 최종 다이제스트 (여러 묶음일 때만 사용) ---
EDITOR = """역할: 너는 여러 투자 채널의 요약본을 합쳐서, 내가 출근길에 폰으로 빠르게 읽을
최종 다이제스트를 만드는 에디터다. 아래는 채널별로 1차 정리된 요약들이다.

{guard}

[규칙]
1. 같은 내용이 여러 채널에 겹치면 하나로 합치고 끝에 "(여러 채널)" 표시.
2. 숫자·종목명·티커는 1차 요약에 있는 그대로 유지. 새로 만들거나 바꾸지 마라.
3. "확실하지 않음" 표시된 건 지우지 말고 그대로 살려라.
4. 중요도 순으로 정리. 안 중요한 건 과감히 생략.

[출력 형식] (한국어, 폰에서 읽기 좋게 짧고 스캔하기 쉽게)

📌 오늘의 핵심 (가장 중요한 3~5개, 한 줄씩)
- ...

📁 주제별 상세  (예: 반도체 / 매크로 / 환율 / 개별종목)
[주제]
- HH:MM  내용 (차트 설명 포함)
- ...

⚠️ 확인 필요 ("확실하지 않음" 항목들)
- ...

[길이] 항목당 1~2줄. 간결하게.
""".replace("{guard}", GUARD)
