"""Gemini에 보내는 두 단계 프롬프트.

GUARD 한 줄을 두 프롬프트 모두에 넣어, 입력이 비었을 때
사람에게 붙여넣기를 요청하지 않고 '오늘은 새 메시지 없음'만 출력하게 한다.
(실제로는 main.py가 코드 레벨에서도 빈 입력을 먼저 차단한다 — 이중 안전장치.)

★출력은 주제 블록들을 `@@@` 한 줄로 구분한다. deliver.py가 이 `@@@`로 잘라
   '주제 블록당 텔레그램 메시지 1개'로 보낸다(폰에서 읽기 좋게).
"""

GUARD = (
    "[중요] 이 입력은 사람이 아니라 자동 파이프라인에서 넘어온 것이다. "
    "입력이 비어 있거나 정리할 메시지가 하나도 없으면, 사용자에게 무엇을 붙여넣어 달라고 "
    "요청하지 말고 정확히 '오늘은 새 메시지 없음' 한 줄만 출력하라."
)

# 두 프롬프트가 공유하는 '자세하게 + 중복 없이 + @@@로 나눠서' 규칙
STYLE = """[상세도 규칙 — 자세하게 쓴다]
- 맨 위 '오늘 한눈에'는 헤드라인만 짧게(각 한 줄, 3~5개). 여기선 절대 자세히 쓰지 마라.
- 주제 블록의 각 항목은 2~4줄로 충분히 풀어써라. 한 항목에 다음을 담아라:
  ① 무슨 일인지(결론 먼저)  ② 핵심 숫자(가격·%·목표가·기간 등, 괄호로)
  ③ 왜 중요한지 / 시사점 / 채널의 해석·전망(원문에 있으면).
- 단, 원문에 없는 사실·수치·해석을 지어내지 마라. 근거가 없으면 ③은 생략하고 사실만.
- 종목명·티커·숫자는 원문 그대로. 어려운 약어는 괄호로 짧게 보충.
- 만연체(꼬리에 꼬리 무는 한 문장)는 금지 — 짧은 문장 2~4개로 나눠 써라.

[중복 금지 — 매우 중요]
- 같은 사건이 여러 채널/여러 글에 나오면 반드시 하나로 합쳐라. 같은 내용을 두 번 쓰지 마라.
  (합칠 때 채널마다 다른 근거·수치가 있으면 그건 모아서 더 풍부하게 만들어라.)
- 맨 위 '오늘 한눈에'와 아래 주제 블록이 같은 문장을 반복하지 않게(위=헤드라인, 아래=상세).
- 아래 주제 블록끼리도 같은 항목을 중복해서 넣지 마라. 한 항목은 가장 알맞은 블록 한 곳에만.

[출력 형식] — 아래 블록들을 이 순서로. 블록과 블록 사이에는 반드시 `@@@` 한 줄만 넣어 구분한다.
(`@@@` 는 블록 사이에만. 블록 안에는 절대 넣지 마라. 내용이 없는 블록은 그 블록과 짝이 되는 @@@까지 통째로 생략.)

🔑 오늘 한눈에
- (가장 중요한 3~5개, 각 한 줄 헤드라인만. 숫자 최소화)
@@@
🌍 지정학·매크로
- HH:MM  내용 (숫자는 괄호로)
@@@
🤖 AI·빅테크
- HH:MM  내용
@@@
💾 반도체
- HH:MM  내용
@@@
🇰🇷 국내·개별종목
- HH:MM  내용
@@@
⚠️ 확인 필요
- 내용 (확실하지 않음)

(주제 블록 이름/이모지는 내용에 맞게 바꾸거나 추가해도 된다. 단 블록 구분은 항상 `@@@` 한 줄.)"""

# --- 1차: 채널 하나의 24시간 글 정리 (여러 묶음일 때만 사용하는 중간 단계) ---
PER_CHANNEL = """역할: 너는 텔레그램 투자 채널 하나의 지난 24시간 글을 정리하는 분석가다.
아래는 [채널명] 채널의 메시지(텍스트 + 차트 이미지)다. 핵심만 구조화해 정리해라.

{guard}

[입력 형식]
- 각 메시지는 `--- HH:MM ---` 로 구분된다.
- 차트/사진은 해당 메시지 바로 뒤에 첨부돼 있다.

[규칙]
1. 광고·홍보·가입유도·잡담·인사말은 제외.
2. 원문의 핵심 숫자(가격, %, 목표가 등)와 종목명·티커는 절대 바꾸지 말고 그대로 보존.
3. 요약은 하되 의미를 왜곡하거나 없는 내용을 만들지 마라.
4. 차트 이미지는 '보이는 것만' 1~2줄로: 종목/지수, 추세, 지지·저항, 거래량, 패턴.
   이미지에서 확인 안 되는 수치는 추측 금지. 애매하면 "확실하지 않음" 표기.
5. 각 항목에 원문 시간(HH:MM) 보존.

[출력 형식]
채널: [채널명]
- HH:MM | 핵심 내용(숫자·종목명 보존) | 차트: (있으면 차트 설명)
- ...
(중요하지 않은 건 생략. 남길 게 없으면 "특이사항 없음")
""".replace("{guard}", GUARD)


# --- 한방 호출: 여러 채널 원문을 한 번에 받아 최종 다이제스트 생성 (기본 경로) ---
COMBINED = """역할: 너는 여러 텔레그램 투자 채널의 지난 24시간 글(텍스트 + 차트 이미지)을 받아,
내가 폰으로 빠르게 읽을 최종 다이제스트를 만드는 분석가 겸 에디터다.

{guard}

[입력 형식]
- 채널은 `### 채널: 이름` 으로 구분된다.
- 각 메시지는 `--- HH:MM ---` 로 구분되고, 차트/사진은 해당 메시지 바로 뒤에 첨부돼 있다.

[내용 규칙]
1. 광고·홍보·가입유도·잡담·인사말은 제외.
2. 핵심 숫자(가격, %, 목표가 등)와 종목명·티커는 절대 바꾸지 말고 그대로 보존.
3. 요약하되 의미를 왜곡하거나 없는 내용을 지어내지 마라.
4. 차트 이미지는 '보이는 것만' 반영(종목/추세/지지·저항/거래량). 확인 안 되면 추측 금지, 애매하면 "확실하지 않음".

{style}
""".replace("{guard}", GUARD).replace("{style}", STYLE)


# --- 2차: 채널별 1차 요약을 합쳐 최종 다이제스트 (여러 묶음일 때만 사용) ---
EDITOR = """역할: 너는 여러 투자 채널의 요약본을 합쳐서, 내가 폰으로 빠르게 읽을
최종 다이제스트를 만드는 에디터다. 아래는 채널별로 1차 정리된 요약들이다.

{guard}

[내용 규칙]
1. 숫자·종목명·티커는 1차 요약에 있는 그대로 유지. 새로 만들거나 바꾸지 마라.
2. "확실하지 않음" 표시된 건 지우지 말고 그대로 살려라.

{style}
""".replace("{guard}", GUARD).replace("{style}", STYLE)
